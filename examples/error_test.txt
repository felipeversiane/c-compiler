// Este arquivo contém erros propositais para testar o sistema de tratamento de erros

// Erro 1: Função sem palavra-chave 'funcao'
__teste() {
    retorno 0;
}

// Erro 2: Variável sem prefixo '!'
funcao __soma(inteiro x, inteiro !y) {
    retorno !x + !y;
}

// Erro 3: Função com prefixo incorreto
funcao soma() {
    retorno 0;
}

// Erro 4: Tipo de dado inválido
funcao __teste2() {
    bool !flag = verdadeiro;
    retorno !flag;
}

// Erro 5: String sem fechar
principal() {
    /* Teste de estouro de memória */
    texto !str1[1000];
    texto !str2[1000];
    texto !str3[1000];
    texto !str4[1000];
    texto !str5[1000];

    /* Tentar alocar mais strings */
    !str1 = "Primeira string muito grande...";
    !str2 = "Segunda string muito grande...";
    !str3 = "Terceira string muito grande...";
    !str4 = "Quarta string muito grande...";
    !str5 = "Quinta string muito grande...";

    /* Tentar alocar mais uma string - deve estourar a memória */
    texto !str6[1000];
    !str6 = "Esta alocação deve falhar por falta de memória";

    retorno 0;
} 