// Este arquivo contém erros propositais para testar o sistema de tratamento de erros

// Erro 1: Função sem palavra-chave 'funcao'
__teste() {
    retorno 0;
}

// Erro 2: Variável sem prefixo '!'
funcao __soma(inteiro x, inteiro !y) {
    retorno !x + !y;
}

// Erro 3: Função com prefixo incorreto
funcao soma() {
    retorno 0;
}

// Erro 4: Tipo de dado inválido
funcao __teste2() {
    bool !flag = verdadeiro;
    retorno !flag;
}

// Erro 5: String sem fechar
principal() {
    texto !mensagem = "Teste de erro;
    
    // Erro 6: Variável não declarada
    !valor = 10;
    
    // Erro 7: Dimensão decimal inválida
    decimal !preco[2.a];
    
    // Erro 8: Parênteses não fechado
    se(!valor > 0 {
        escreva("Positivo");
    }
    
    // Erro 9: Operador inválido
    inteiro !a = 5 $ 2;
    
    // Erro 10: Chamada de função inexistente
    __nao_existe();
    
    // Erro 11: Tipo incompatível em atribuição
    texto !nome = 123;
    
    // Erro 12: Dimensão negativa
    texto !texto[-10];
    
    // Erro 13: Chaves não fechadas
    para(!i = 0; !i < 10; !i = !i + 1) {
        escreva(!i);
    
    retorno 0;
} 